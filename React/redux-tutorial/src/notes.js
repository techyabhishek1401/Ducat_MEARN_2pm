//--------------------------Redux--------------------------

/**
 Actions: these are the objects that should have two properties.
         1)Type->describe type of action
         2)payload(data)->describe what should be change in store
        (objects)
Reducers: these are the functions that implementhe behavior of the actions.They change the satte of the           app      based on the action description.
(onChange)

Store: it brings the actions and reducers together (central state)(this.state)
 */